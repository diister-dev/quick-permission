name: CI
on:
  push:
    branches:
      - main
      - develop
      - release/*
  pull_request:
    branches:
      - main
      - develop
      - release/*

defaults:
  run:
    working-directory: library

jobs:
  static:
    name: Typecheck, lint and build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run check

      - name: Lint
        run: bun run lint

      - name: Format
        run: bun run fmt:check

      - name: Build
        run: bun run build

  test:
    name: Tests (${{ matrix.runtime }})
    runs-on: ubuntu-latest

    # The suite targets `node:test`, so the same tests run on all three.
    # Worth the matrix: Bun is JavaScriptCore, Node and Deno are V8.
    strategy:
      fail-fast: false
      matrix:
        runtime: [bun, node, deno]

    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - uses: actions/setup-node@v7
        with:
          node-version: 22.18

      - uses: denoland/setup-deno@v2
        if: matrix.runtime == 'deno'
        with:
          deno-version: v2.9.6

      - run: bun install --frozen-lockfile

      - name: Test (bun)
        if: matrix.runtime == 'bun'
        run: bun test

      - name: Test (node)
        if: matrix.runtime == 'node'
        run: node --test "test/*_test.ts"

      - name: Test (deno)
        if: matrix.runtime == 'deno'
        run: deno test -A --no-check test/

  consumers:
    name: Consume the built package (${{ matrix.runtime }})
    runs-on: ubuntu-latest
    needs: static

    # Installs the real tarball into an empty project — the only thing that
    # exercises the `exports` map and the generated `.d.ts` the way a user does.
    strategy:
      fail-fast: false
      matrix:
        runtime: [bun, node]

    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - uses: actions/setup-node@v7
        with:
          node-version: 22.18

      - run: bun install --frozen-lockfile

      - name: Build and pack
        run: |
          bun run build
          npm pack --pack-destination /tmp

      - name: Install into an empty project
        working-directory: /tmp
        run: |
          mkdir -p consumer && cd consumer
          echo '{"name":"consumer","type":"module","private":true}' > package.json
          npm install /tmp/diister-quick-permission-*.tgz

      - name: Load the package and exercise it
        working-directory: /tmp/consumer
        run: |
          cat > check.js <<'EOF'
          import * as qp from "@diister/quick-permission";
          for (const name of ["createSystem", "permission", "resource"]) {
            if (typeof qp[name] !== "function") {
              throw new Error(`${name} did not load`);
            }
          }
          console.log("entry point loaded:", Object.keys(qp).length, "exports");
          EOF
          ${{ matrix.runtime }} check.js
